import {getIcon} from "@/components/ultility/Ultility";

export default async function SidebarBody(props:any) {
	// const  Icon = await getIcon(props.data.library, props.data.icon)

	return (
		<div>

		</div>
	)
}