/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
        extend: {
            // You can extend the default theme here
            colors: {
                'brand-blue': '#1992d4',
            },
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
            },
        },
    },
    plugins: [],
}